# 前端缺失页面 — 待审批方案汇总

**汇总人：** Aemeath Manager  
**日期：** 2026-04-16  
**状态：** 🟡 待 Edward 审批

---

## 一、需求摘要

对照需求文档《前端缺失页面清单》，本次涉及：
- **3 个新建页面**
- **4 个残缺页面补全**

---

## 二、模块变更列表

| 页面 | 类型 | 文件路径 |
|------|------|---------|
| 音源详情页 | 新建 | `pages/audio-sources/details.html` |
| 录音详情页 | 新建 | `pages/recordings/detail.html` |
| 转写历史页 | 新建 | `pages/transcribe/history.html` |
| 歌曲详情页 | 补全 | `pages/songs/details.html` |
| 音源列表页 | 补全 | `pages/audio-sources/index.html` |
| 录音列表页 | 补全 | `pages/recordings/index.html` |
| 首页 | 补全 | `index.html` |

---

## 三、前端方案摘要

来源文档：`/home/realllyka/project/music_chrod_project/docs/frontend/2026-04/前端缺失页面设计方案.md`

### 3.1 新建页面

**音源详情页** `pages/audio-sources/details.html`
- 基本信息卡片（ID、名称、格式、大小、状态、上传时间）
- 音频播放预览（`<audio>` 标签，src 指向 `file_path`）
- 删除按钮 + 返回列表按钮

**录音详情页** `pages/recordings/detail.html`
- 基本信息卡片（session_id、文件名、时长、采样率、状态、创建时间）
- 音频播放预览
- 编辑文件名（`audio_name` 字段）
- 删除录音 + 返回列表

**转写历史页** `pages/transcribe/history.html`
- 表格展示某歌曲所有转写任务（task_id、mode、status、created_at、result_path）
- 已完成任务提供下载链接（`result_path`）
- 失败任务展示错误信息
- 从歌曲详情页以 `song_id` 参数跳转进入

### 3.2 补全页面

**歌曲详情页** — 新增：
- 转写结果卡片：`melody_path` / `chord_path` 有值则显示下载链接，否则显示"未生成"
- 快速提取按钮（melody / chord），调用 `/api/transcribe/start`
- 查看转写历史链接

**音源列表页** — 新增：
- 每行"详情"按钮 + "删除"按钮
- 文件大小、上传时间列

**录音列表页** — 新增：
- 每行"详情"按钮，跳转到 `/recordings/detail?id=X`

**首页** — 新增：
- 快捷入口卡片（8 个功能入口）
- 统计卡片（依赖 `/api/status` 统计字段，**当前不可用，见风险项**）

---

## 四、后端方案摘要

> **字段命名规则**：以后端 API 返回字段为准，前端在实现层做映射。

### 4.1 API 确认结论

来源文档：`/home/realllyka/project/music_chrod_project/docs/backend/2026-04/前端缺失页面-API确认报告.md`

### 4.1 API 确认结论

| API | 状态 | 说明 |
|-----|------|------|
| `GET /api/audio-sources/<id>` | ✅ 可用 | 返回字段需映射（见下方） |
| `DELETE /api/audio-sources/<id>` | ✅ 可用 | 删除成功返回 `{ok: true}` |
| `GET /api/capture/detail/<session_id>` | ✅ 可用 | 返回字段需映射（见下方） |
| `PUT /api/capture/sessions/<session_id>` | ✅ 可用 | 仅支持 `audio_name` 字段 |
| `DELETE /api/capture/sessions/<session_id>` | ✅ 可用 | 删除文件 + DB 记录 |
| `GET /api/transcribe/song/<song_id>` | ✅ 可用 | 返回 `{tasks: [...]}`，用 `result_path` |
| `GET /api/songs/<song_id>` | ✅ 可用 | 确认返回 `melody_path/chord_path/melody_url/chord_url` |
| `GET /api/status` | ⚠️ 部分可用 | 仅返回健康状态，**无统计字段** |

### 4.2 字段映射对照（前端实现时处理，以后端字段为准）

| 接口 | 前端原本期望字段 | 后端实际字段 | 前端处理方式 |
|------|-------------|-------------|---------|
| 音源详情 | `name` | `audio_name` | 直接使用 `audio_name` |
| 音源详情 | `size` | `file_size` | 直接使用 `file_size` |
| 音源详情 | `upload_time` | `created_at` | 直接使用 `created_at` |
| 录音详情 | `filename` | `audio_name` | 直接使用 `audio_name` |
| 录音详情 | `duration` | `duration_sec` | 直接使用 `duration_sec` |
| 更新录音 | `filename` 入参 | `audio_name` | 前端传 `audio_name` 字段 |

---

## 五、API 对接表（字段命名规则：以后端为准）

> **重要规则**：前后端字段不一致时，**以后端 API 实际返回字段为准**，前端实现时做字段映射处理，不推动后端修改字段名。

| 页面 | API | 方法 | 请求参数 | 响应关键字段 |
|------|-----|------|---------|------------|
| 音源详情 | `/api/audio-sources/<id>` | GET | path: id | audio_name, file_size, format, created_at, file_path |
| 音源详情/列表 | `/api/audio-sources/<id>` | DELETE | path: id | {ok: true} |
| 录音详情 | `/api/capture/detail/<session_id>` | GET | path: session_id | audio_name, duration_sec, file_path |
| 录音详情 | `/api/capture/sessions/<session_id>` | PUT | body: {audio_name} | {ok: true, audio_name} |
| 录音详情/列表 | `/api/capture/sessions/<session_id>` | DELETE | path: session_id | {ok: true} |
| 转写历史 | `/api/transcribe/song/<song_id>` | GET | path: song_id | {tasks: [{task_id, mode, status, result_path, created_at}]} |
| 转写历史 | `/api/songs/<song_id>` | GET | path: song_id | title, melody_path, chord_path, melody_url, chord_url |
| 歌曲详情 | `/api/transcribe/start` | POST | body: {song_id, mode} | {ok, task_id} |
| 首页 | `/api/status` | GET | — | status, database（**无统计字段**） |

---

## 六、数据库变更

**无数据库结构变更。** 本次仅涉及前端页面新增/补全，后端 API 已全部存在。

---

## 七、风险点与待确认项

| 风险项 | 级别 | 说明 | 处理建议 |
|--------|------|------|---------|
| 首页统计卡片 | ⚠️ 中 | `/api/status` 不返回统计字段 | 方案A：前端先隐藏统计区；方案B：后端先补 `/api/status` 统计字段 |
| 字段名不一致 | ✅ 已解决 | 前端期望 name/size/filename，后端返回 audio_name/file_size 等 | **已确定：以后端字段为准，前端实现时映射** |
| 音源被引用不可删除 | ℹ️ 已知 | 删除会返回错误 | 前端需处理错误提示 |
| 转写结果下载 | ℹ️ 已知 | 转写历史用 `result_path`，歌曲详情用 `melody_url/chord_url` | 前端按场景分别使用 |

---

## 八、验收清单汇总

### 前端验收
- [ ] 7 个页面全部实现并可访问
- [ ] 所有列表页可跳转详情页
- [ ] 所有详情页有返回入口
- [ ] 删除操作有二次确认
- [ ] API 错误有友好提示
- [ ] 字段映射正确，无空值问题
- [ ] 转写结果可正常下载

### 后端验收
- [ ] 8 个 API 响应字段符合对接表
- [ ] 删除接口返回 `{ok: true}`
- [ ] 歌曲详情返回 `melody_path/chord_path/melody_url/chord_url`
- [ ] 转写历史返回任务列表结构
- [ ] （可选）`/api/status` 补充统计字段（影响首页统计卡片）

---

## 九、工作流程

```
✅ 需求分析 → ✅ 任务派发 → ✅ Backend 分析 → ✅ Frontend 分析
✅ 对齐检查 → ✅ 汇总本方案 → ⏳ Edward 审批 → ⬜ 释放执行
```

---

## 十、下一步

**等待 Edward 审批。批准后我释放执行信号，backend 和 frontend 各自开始实现。**

---

_本文件由 Aemeath Manager 自动生成_
