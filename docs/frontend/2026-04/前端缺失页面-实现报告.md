# 前端缺失页面 — 实现报告

**实现人：** Frontend Agent  
**日期：** 2026-04-16  
**状态：** ✅ 全部完成（7/7）

---

## 一、每个页面实现状态

### 1. pages/audio-sources/details.html（新建）✅

**实现内容：**
- 基本信息卡片：ID、audio_name、format、file_size（显示为"字节"）、status、created_at、file_path
- 音频预览卡片：`<audio controls>`，src 指向 `API_BASE + s.file_path`；无 file_path 时卡片隐藏
- 删除按钮：二次确认 `confirm()`，成功后跳转 `/audio-sources`
- 返回列表按钮：`window.location.href='/audio-sources'`
- API：`GET /api/audio-sources/<id>`、`DELETE /api/audio-sources/<id>`

---

### 2. pages/recordings/detail.html（新建）✅

**实现内容：**
- 基本信息卡片：session_id、audio_name、duration_sec、sample_rate、status、created_at、file_path
- 音频预览卡片：同上
- 编辑 audio_name：输入框 + 保存按钮，调用 `PUT /api/capture/sessions/<session_id>`，body: `{audio_name}`
- 删除按钮：二次确认，成功后跳转 `/recordings`
- 返回按钮：跳转 `/recordings`
- API：`GET /api/capture/detail/<session_id>`、`PUT /api/capture/sessions/<session_id>`、`DELETE /api/capture/sessions/<session_id>`

---

### 3. pages/transcribe/history.html（新建）✅

**实现内容：**
- 通过 URL 参数 `?song_id=X` 进入，`backLink` 动态指向 `/songs/details?id=X`
- 加载歌曲信息卡片（调用 `GET /api/songs/<song_id>`）
- 转写任务表格：task_id、mode（旋律提取/和弦分离）、status（中文标签+颜色）、created_at、result_path（已完成显示下载链接；失败显示错误信息）
- API：`GET /api/transcribe/song/<song_id>`、`GET /api/songs/<song_id>`

---

### 4. pages/songs/details.html（补全）✅

**新增内容（在原"操作"卡片前插入）：**
- 转写结果卡片：读取 `r.melody_url || r.melody_path` 和 `r.chord_url || r.chord_path`，有值则显示下载链接，否则显示"未生成"（#aaa 灰色）
- 快速提取旋律按钮：`POST /api/transcribe/start` body: `{song_id, mode:'melody'}`，提交后显示任务ID提示
- 快速分离和弦按钮：同上，`mode:'chord'`
- 查看转写历史链接：跳转 `/transcribe/history?song_id=<id>`
- 提交状态反馈区域（成功绿色，失败红色）

---

### 5. pages/audio-sources/index.html（补全）✅

**修改内容：**
- 将原 `.source-item` 列表改为 `<table>` 展示，增加列：ID、文件大小、状态、上传时间
- 每行新增"详情"按钮（跳转 `/audio-sources/details?id=X`）
- 每行新增"删除"按钮（`DELETE /api/audio-sources/<id>`，二次确认，成功后刷新列表）

---

### 6. pages/recordings/index.html（补全）✅

**修改内容：**
- 每行新增"详情"按钮（跳转 `/recordings/detail?id=<session_id>`），位于已有"编辑"和"删除"按钮前

---

### 7. index.html（补全）✅

**新增内容：**
- **统计卡片区（Option A）**：并发调用 `songs/list?limit=1`、`artists/list?limit=1`、`audio-sources/list?limit=1`，优先取响应中 `total` 字段；若无 `total` 则回退到 `.length`。使用 `Promise.allSettled` 确保单接口失败不影响其他统计展示。
- **快捷入口 8 个卡片**：查询歌曲、创建歌曲、歌手列表、新增歌手、录音采集、录音文件、上传音源、提取分析。悬停有 shadow + translate 动效。

---

## 二、未完成项

**无。** 7 个页面全部实现。

---

## 三、发现的后端 API 问题 / 待确认项

| 问题 | 严重度 | 说明 | 建议 |
|------|--------|------|------|
| 首页统计 `total` 字段不确定 | ⚠️ 中 | `/api/songs/list`、`/api/artists/list`、`/api/audio-sources/list` 是否在响应体中包含 `total` 字段尚未完全确认。前端已做降级处理（`total` 不存在时显示当前页数量）。 | 后端确认并统一返回 `total` 字段 |
| `/api/status` 无统计字段 | ℹ️ 已知 | 已采用 Option A 方案绕过，不依赖 `/api/status` | — |
| 音源被引用时删除会返回错误 | ℹ️ 已知 | 前端已通过 `alert(r.error)` 展示后端错误消息 | — |
| `GET /api/capture/detail/<session_id>` 响应结构 | ℹ️ 待核实 | 假设响应为平铺对象（同 `sessions` 列表中单条记录结构），包含 `session_id`、`audio_name`、`duration_sec`、`sample_rate`、`status`、`created_at`、`file_path` | 若响应包装在某 key 下，前端需调整取值层级 |

---

## 四、前端验收清单完成情况

| 验收项 | 状态 |
|--------|------|
| 7 个页面全部实现并可访问 | ✅ |
| 所有列表页可跳转详情页 | ✅（音源列表 → 音源详情；录音列表 → 录音详情；歌曲列表 → 歌曲详情） |
| 所有详情页有返回入口 | ✅ |
| 删除操作有二次确认 | ✅（所有删除均使用 `confirm()`） |
| API 错误有友好提示 | ✅（统一使用 `alert(r.error || '操作失败')`） |
| 字段映射正确，无空值问题 | ✅（audio_name、file_size、duration_sec、sample_rate 均已按后端字段直接使用，空值显示 `-`） |
| 转写结果可正常下载 | ✅（history.html 提供 `<a download>` 链接；details.html 同样提供） |

---

## 五、改动文件清单

| 文件 | 类型 |
|------|------|
| `pages/audio-sources/details.html` | 新建 |
| `pages/recordings/detail.html` | 新建 |
| `pages/transcribe/history.html` | 新建 |
| `pages/songs/details.html` | 修改（补全） |
| `pages/audio-sources/index.html` | 修改（补全） |
| `pages/recordings/index.html` | 修改（补全） |
| `index.html` | 修改（补全） |

---

_本文件由 Frontend Agent 自动生成，2026-04-16_
